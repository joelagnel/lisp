;;helpers


(defun catalyst-new-app ()
  "catalyst.pl"
  (interactive))

(defun catalyst-model-add ()
  "Add a new model"
  (interactive))

(defun catalyst-view-add ()
  "Add a view"
  (interactive))

(defun catalyst-controller-add ()
  "Add a controller"
  (interactive))

(provide 'helper)