
(defun catalyst-server-start ()
  "Start standalone server. Ask for port"
  (interactive))


(defun catalyst-server-stop ()
  "Stop standalone server"
  (interactive))

(defun catalyst-server-restart ()
  "Restart serve"
  (interactive))


(defun catalyst-server-status ()
  "Server status on modeline!"
  (interactive))

(defun catalyst-log ()
  "View dumper log"
  (interactive))

(defun catalyst-log-grep ()
  "Grep log"
  (interactive))



(provide 'catalyst-server)