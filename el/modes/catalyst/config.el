(defun catalyst-config-yaml ()
  "take to app.yml"
  (interactive))

(defun catalyst-config-pm ()
  "take to app.pm"
  (interactive))

(defun catalyst-config-xml ()
  "take to xml.pm"
  (interactive))



(provide 'config)