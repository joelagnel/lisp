
;;;AUCTEX    


(load "de-vars")
(setq load-path (cons (concat root-path "docu/auctex") load-path))


(require 'tex-site)

(setq load-path (cons (concat root-path "docu") load-path))


(setq woman-use-own-frame nil)

;;read word/doc files
(require 'no-word)


